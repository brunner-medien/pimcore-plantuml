{{ '"' | raw }}
    {%- if not(class.title) %}
        {{- class.name }}
    {%- elseif translation == 'none' or class.isAssociationClass %}
        {{- class.title }}
    {%- else %}
        {{- class.title | trans([], 'admin', translation) | capitalize }}
    {%- endif %}
{{ '"' | raw -}}
